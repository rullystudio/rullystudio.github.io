git remote set-url origin git@github.com:rullystudio/rullystudio.github.io.git     -> remote ssh
git pull origin main --rebase ->ambil branch agar sinkron
git push origin main

git rm -r css //hapus folder
git commit -m "Hapus folder byd di luar assets"
git push origin main / git push --set-upstream origin main
